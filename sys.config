[
  {kernel,
    [
      {logger,
        [
          {handler, default, logger_std_h,
            #{
              config => #{file => "logs/application.log"},
              level => info
            }
          },
          {handler, console, logger_std_h,
            #{
              config => #{type => standard_io},
              level => info
            }
          }
        ]
      }
    ]
  }
].